// content.js
// No immediate inline injection needed. twikit.js blocks interactions after injection.
(() => {
  // Placeholder: could listen for a class or storage flag to hide UI, but overlay is enough.
})();
